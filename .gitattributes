# Normalize line endings to LF in the repository for all text files.
* text=auto eol=lf

# Slint source files — always LF
*.slint text eol=lf

# Markdown — always LF
*.md text eol=lf

# Binary files — no line ending conversion
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg binary
